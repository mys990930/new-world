# ocean

## Role

- Shape negative-height sea floors for `DeepOcean` and `Shelf` profiles.

## Notes

- `DeepOcean` prioritizes basin depth and broader undulation.
- `Shelf` stays shallower and transitions toward coast without collapsing to a single flat `y`.
